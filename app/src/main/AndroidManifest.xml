<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tunisair.khawla.tunisair">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InscriptionActivity" />
        <activity
            android:name=".AccountActivity"
            android:label="@string/title_activity_account"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MouvementActivity"
            android:label="@string/title_activity_mouvement"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ProfilActivity"
            android:label="@string/title_activity_profil"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BilletActivity"
            android:label="@string/title_activity_billet"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MilesActivity"
            android:label="@string/title_activity_miles"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ReclamationActivity"
            android:label="@string/title_activity_reclamation"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>